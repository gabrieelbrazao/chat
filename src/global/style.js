import { DefaultTheme } from "react-native-paper";

const globalTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#57479E"
  }
};

export default globalTheme;
